# Core
google-adk>=1.0.0
google-genai>=1.0.0

# Scraping — default path
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Config / environment
python-dotenv>=1.0.0

# Playwright — only needed as fallback for JavaScript-rendered pages.
# After installing, run once: playwright install chromium
playwright>=1.44.0
